# Shortly Frontend

Next.js dashboard for the Shortly URL shortener. See `AGENTS.md` (this folder)
for the frontend spec and `../AGENTS.md` for the backend/system design.

```bash
npm install
npm run dev     # http://localhost:3001 (backend on :3000)
npm test
npm run build
```
